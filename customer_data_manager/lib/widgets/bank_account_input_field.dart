import 'package:flutter/material.dart';

class BankAccountInputField extends StatelessWidget {
  const BankAccountInputField({super.key});

  @override
  Widget build(BuildContext context) {
    return const TextField();
  }
}